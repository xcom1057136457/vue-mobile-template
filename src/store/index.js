import Vue from 'vue'
import Vuex from 'vuex'
import scrollInfo from './module/scrollInfo'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    scrollInfo
  }
})
